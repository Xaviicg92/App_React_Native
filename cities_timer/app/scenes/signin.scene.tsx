import { Alert } from "react-native";
import { signin } from "../pods/signin/api/signin.api";
import { SigninContainer } from "../pods/signin/signin.container";

export const SigninScene = () => {
  return <SigninContainer />;
};
